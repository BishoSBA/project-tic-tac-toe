const gameBoard = (() => {
	const board = new Array(9);
})();

const displayController = () => {
	const update = () => {
		const htmlBoard = document.querySelector("main");
		const cells = [];
	};
};
